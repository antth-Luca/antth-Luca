# Nome da Actions:  
name: Snake Game

# Controlador do tempo que sera feito a atualização dos arquivos.
on:
  schedule:
      # Será atualizado a cada 5 horas.
    - cron: "0 */5 * * *"

# Permite executar na na lista de Actions (utilizado para testes de build).
  workflow_dispatch:

# Regras
jobs:
  build:
    runs-on: ubuntu-latest
    steps:

    # Checks repo under $GITHUB_WORKSHOP, so your job can access it
      - uses: actions/checkout@v2

    # Repositorio que será utilizado para gerar os arquivos.
      - uses: Platane/snk@master
        id: snake-gif
        with:
          github_user_name: nomeUsuario #Seu usuario
          gif_out_path: dist/github-contribution-grid-snake.gif
          svg_out_path: dist/github-contribution-grid-snake.svg

      - run: git status

      # Para as atualizações.
      - name: Push changes
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: master
          force: true

      - uses: crazy-max/ghaction-github-pages@v2.1.3
        with:
          # the output branch we mentioned above
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
Retornando na opção Actions do repositório, clique em “Run workflow”:

repositório

Pasta dos arquivos:
Em seu repositório cliente em main e selecione a saída branch:
saída

E aqui estão os arquivos gerados que você pode usar como bem entender:

Arquivos

Codigo Github para uso:

![snake gif](https://github.com/SEU_USUARIO/SEU_REPOSITORIO/blob/output/github-contribution-grid-snake.svg)

Principais comentários (0)

Se inscrever
foto
Adicionar à discussão
Código de Conduta • Relatar abuso
Queremos sua ajuda!  Torne-se um moderador de tags.
Preencha esta pesquisa e ajude-nos a moderar nossa comunidade, tornando-se um moderador de tags aqui no DEV.

Leia a seguir
imagem de perfil balastrong
Executando testes automatizados com ações do GitHub
Leonardo Montini -3 de janeiro

joerismits imagem de perfil
Garanta que o desempenho do seu aplicativo Next.js seja excelente com o Lighthouse CI e o GitHub Actions
Joeri Smits -3 de janeiro

imagem de perfil dssinyukov
Verificando confirmações do Git usando GPG
Denis Sinyukov - Jan 1

imagem de perfil arisamiga
Making integration between RSS and LinkedIn
Aris - Nov 29 '22


Henrique Lopes
Follow
"Não há razão alguma em usar a palavra “impossível” para descrever algo que claramente pode acontecer."
JOINED
5 de set. de 2021
Trending on DEV Community 👩‍💻👨‍💻 
Imagem de perfil de Vincent A. Cicirello
Who likes stickers more? Software developers/computing professionals or five-year-olds? And why?
#discuss #watercooler #hacktoberfest #meta
Lucy Linder imagem de perfil
I challenged myself to read every day, and it changed my life 📚
#watercooler #reading #productivity #motivation
Nick Taylor imagem de perfil
Twitter cobrando pelo uso da API a partir de 9 de fevereiro
# twitter # api # discutir
# Nome da Actions:  
name: Snake Game

# Controlador do tempo que sera feito a atualização dos arquivos.
on:
  schedule:
      # Será atualizado a cada 5 horas.
    - cron: "0 */5 * * *"

# Permite executar na na lista de Actions (utilizado para testes de build).
  workflow_dispatch:

# Regras
jobs:
  build:
    runs-on: ubuntu-latest
    steps:

    # Checks repo under $GITHUB_WORKSHOP, so your job can access it
      - uses: actions/checkout@v2

    # Repositorio que será utilizado para gerar os arquivos.
      - uses: Platane/snk@master
        id: snake-gif
        with:
          github_user_name: antth-Luca
          gif_out_path: dist/github-contribution-grid-snake.gif
          svg_out_path: dist/github-contribution-grid-snake.svg

      - run: git status

      # Para as atualizações.
      - name: Push changes
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: master
          force: true

      - uses: crazy-max/ghaction-github-pages@v2.1.3
        with:
          # the output branch we mentioned above
          target_branch: output
          build_dir: dist
